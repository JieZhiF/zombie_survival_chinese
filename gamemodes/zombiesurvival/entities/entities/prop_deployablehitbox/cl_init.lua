-- ============================================================================
-- prop_deployablehitbox/cl_init.lua - 可部署物命中盒（客户端）
-- 负责：隐藏命中盒的模型与阴影，使其在客户端完全不可见，仅保留服务器端命中判定
-- ============================================================================
INC_CLIENT()

-- ==== Initialize - 初始化命中盒：关闭阴影绘制并设为不可见 ====
function ENT:Initialize()
	self:DrawShadow(false)
	self:SetNoDraw(true)
end
