-- ============================================================================
-- status_bleed/cl_init.lua - 流血状态（客户端）
-- 负责：将本状态实体注册到拥有者身上，供 HUD 血条/图标显示引用
-- ============================================================================
INC_CLIENT()

-- ==== Draw - 无自定义绘制（本体为纯状态数据） ====
function ENT:Draw()
end

-- ==== Initialize - 初始化：把流血状态引用挂到拥有者（供 HUD 读取） ====
function ENT:Initialize()
	self:GetOwner().Bleed = self
end
