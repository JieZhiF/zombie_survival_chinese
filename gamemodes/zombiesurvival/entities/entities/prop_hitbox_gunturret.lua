-- ============================================================================
-- prop_hitbox_gunturret - 自动炮塔部署碰撞盒
-- 负责：作为可部署自动炮塔（prop_gunturret）的受击判定盒，
--       继承通用部署碰撞盒基类，定义炮塔的大型盒体尺寸
-- ============================================================================

-- 客户端与服务器同时加载本文件
AddCSLuaFile()

-- 实体类型：动画实体，继承可部署碰撞盒基类
ENT.Type = "anim"
ENT.Base = "prop_deployablehitbox"

-- 碰撞盒最小角（底面中心偏置）
ENT.BoxMin = Vector(-20, -16, 0)
-- 碰撞盒最大角（44×32×64 的立方体，覆盖炮塔全身）
ENT.BoxMax = Vector(24, 16, 64)
