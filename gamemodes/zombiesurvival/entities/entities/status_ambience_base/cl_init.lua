-- ============================================================================
-- cl_init.lua - 氛围实体基类（客户端）：占位文件，无独立逻辑
-- 负责：满足实体三件套（shared/init/cl_init）的加载约定；子类在各自
--       cl_init.lua 中实现音效与渲染行为
-- ============================================================================
INC_CLIENT()

-- 氛围实体基类: 无独立客户端逻辑
-- 子类在各自的 cl_init.lua 中定义音效/渲染行为
-- 本文件存在仅为满足 INC_SERVER() 的 AddCSLuaFile("cl_init.lua") 三件套要求
