-- ============================================================================
-- prop_hitbox_camera - 监控摄像头部署碰撞盒
-- 负责：作为可部署监控摄像头（prop_camera）的受击判定盒，
--       继承通用部署碰撞盒基类，定义摄像头的小型盒体尺寸
-- ============================================================================

-- 客户端与服务器同时加载本文件
AddCSLuaFile()

-- 实体类型：动画实体，继承可部署碰撞盒基类
ENT.Type = "anim"
ENT.Base = "prop_deployablehitbox"

-- 碰撞盒最小角（底面中心偏置）
ENT.BoxMin = Vector(-8, -8, 0)
-- 碰撞盒最大角（16×16×8 的扁立方体）
ENT.BoxMax = Vector(8, 8, 8)
